---
/**
 * Sidebar template — main content (2/3) + sidebar column (1/3).
 * Used for blog-style pages, documentation, filtered listings.
 *
 * The sidebar slot is reserved for future sidebar widgets.
 * Currently renders an empty aside that can be populated via props or slots.
 */
---

<div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
  <div class="lg:grid lg:grid-cols-3 lg:gap-8">
    <div class="lg:col-span-2">
      <slot />
    </div>
    <aside class="mt-8 lg:mt-0">
      <slot name="sidebar" />
    </aside>
  </div>
</div>
