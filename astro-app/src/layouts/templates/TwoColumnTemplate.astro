---
/**
 * Two Column template — equal two-column grid.
 * Used for comparison pages and dual-content layouts.
 *
 * Blocks from BlockRenderer become direct grid items and distribute
 * across two equal columns automatically.
 */
---

<div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
  <div class="lg:grid lg:grid-cols-2 lg:gap-8">
    <slot />
  </div>
</div>
