---
/**
 * Landing Page template — minimal navigation distractions, conversion-focused.
 * Full-width blocks with no container constraint.
 * Used for marketing campaigns and event pages.
 *
 * Header/footer are suppressed via Layout's hideNav prop (set in [...slug].astro).
 */
---

<div class="w-full" data-template="landing">
  <slot />
</div>
