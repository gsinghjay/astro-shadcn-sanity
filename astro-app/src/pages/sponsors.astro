---
import Layout from '../layouts/Layout.astro';
import BlockRenderer from '../components/BlockRenderer.astro';
import { sponsorsPage } from '../lib/data';

export const prerender = true;
---

<Layout title={sponsorsPage.title ?? undefined} description={sponsorsPage.description ?? undefined}>
  <BlockRenderer blocks={sponsorsPage.blocks ?? []} />
</Layout>
