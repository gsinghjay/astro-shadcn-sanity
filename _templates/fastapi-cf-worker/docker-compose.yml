services:
  worker:
    build: .
    user: "${UID:-1000}:${GID:-1000}"
    ports:
      - "8787:8787"
    volumes:
      - .:/app
    env_file:
      - path: .dev.vars
        required: false
    command: uv run pywrangler dev --ip 0.0.0.0
